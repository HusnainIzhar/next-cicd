resource "aws_instance" "example" {
  ami           = "ami-0c55b159cbfafe1f0"  # Replace with a valid AMI ID for us-east-1
  instance_type = "t2.micro"               # Instance type (t2.micro is eligible for free tier)
  key_name      = "your-key-name"          # Replace with your existing Key Pair name

  # Configure instance tags
  tags = {
    Name = "example-instance"
  }

  # You can add additional configurations like security groups, user data, etc.
}
